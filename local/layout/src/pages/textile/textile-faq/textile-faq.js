import { initAccordion } from '@/shared/lib';

const textileFaqAccordion = document.querySelector('.textile-faq__accordion-wrapper');

if (textileFaqAccordion) {
	initAccordion(textileFaqAccordion);
}
